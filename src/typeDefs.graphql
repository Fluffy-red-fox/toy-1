scalar Upload

type Query{
    test: String!
}

type Mutation{
    imgUpload(file: Upload!): Boolean!

    kakaoAuth(code: String!): Auth!

    refreshAuth(refreshToken: String!): Auth!

    logout(token: String!): Boolean!
}

type Auth{
    access_token: String!
    refresh_token: String
}